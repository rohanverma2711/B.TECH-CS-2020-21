/* This SQL file will map all the relationships
    between the tables as specified in ER diagram   */
                                               
ALTER TABLE EMPLOYEE
    ADD CONSTRAINT FOREIGN KEY(SuperSSN) REFERENCES EMPLOYEE(SSN),
    ADD CONSTRAINT FOREIGN KEY(DepNo) REFERENCES DEPARTMENT(DepNo);

ALTER TABLE DEPARTMENT
    ADD CONSTRAINT FOREIGN KEY(MgrSSN) REFERENCES EMPLOYEE(SSN);

ALTER TABLE DEPT_LOCATIONS
    ADD CONSTRAINT FOREIGN KEY(DeptNo) REFERENCES DEPARTMENT(DepNo);

ALTER TABLE PROJECT
    ADD CONSTRAINT FOREIGN KEY(DepNo) REFERENCES DEPARTMENT(DepNo);

ALTER TABLE WORKS_ON
    ADD CONSTRAINT FOREIGN KEY(ESSN) REFERENCES EMPLOYEE(SSN),
    ADD CONSTRAINT FOREIGN KEY(PNo) REFERENCES PROJECT(PNumber);

ALTER TABLE DEPENDENT
    ADD CONSTRAINT FOREIGN KEY(ESSN) REFERENCES EMPLOYEE(SSN);